# Tasks: Update config.yaml

## Implementation Tasks

- [x] Write the `context:` block in `openspec/config.yaml` covering tech stack, implemented systems, architecture patterns, coding conventions, and domain knowledge
- [x] Write the `rules:` block in `openspec/config.yaml` with per-artifact rules for `proposal`, `design`, and `tasks`
- [x] Verify the YAML is valid (no syntax errors, correct indentation)
